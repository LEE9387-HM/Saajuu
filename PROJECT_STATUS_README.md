# Saajuu 프로젝트 현황 / 핸드오프 문서

## 현재 버전

- 버전: `v0.5.58.0`
- 기준일: `2026-07-22`
- 배포: `main` 푸시 시 GitHub Pages 자동 배포
- 최근 검증: `npm test`, `npm run build`

## 제품 방향

Saajuu는 브라우저 안에서 사주 계산과 해석을 제공하고, 결과를 오늘 운세, 연간 운세, 궁합, 타로, AI 상담으로 이어 주는 서비스입니다.

핵심 방향은 다음과 같습니다.

1. 무료 콘텐츠만으로도 기본 만족감을 준다.
2. 결과가 길기만 한 운세가 아니라, 실제 고민과 다음 행동으로 이어지게 만든다.
3. 로그인 이후에는 인연 연결, 상담 기록, 결제형 상담으로 자연스럽게 확장한다.

## 현재 아키텍처

### 프런트엔드

- Vite 기반 정적 앱
- 주요 엔트리: [src/main.js](C:\WorkSpace\메모\10. Project\Saajuu\src\main.js)
- 스타일: [src/styles.css](C:\WorkSpace\메모\10. Project\Saajuu\src\styles.css)
- HTML 셸: [index.html](C:\WorkSpace\메모\10. Project\Saajuu\index.html)

### 핵심 도메인 모듈

- 사주/해석/궁합/타로/연간 운세: [src/fortune.js](C:\WorkSpace\메모\10. Project\Saajuu\src\fortune.js)
- 일일 운세 생성: [src/daily.js](C:\WorkSpace\메모\10. Project\Saajuu\src\daily.js)
- 로컬 저장: [src/storage.js](C:\WorkSpace\메모\10. Project\Saajuu\src\storage.js)
- 공유 해시: [src/share.js](C:\WorkSpace\메모\10. Project\Saajuu\src\share.js)
- 인증/상담/인연/Supabase 연동: [src/auth.js](C:\WorkSpace\메모\10. Project\Saajuu\src\auth.js)

### 외부 서비스

- Supabase Auth / Database / Edge Functions
- PortOne 브라우저 SDK
- GitHub Pages

## 현재 구현 상태

### 완료된 사용자 기능

- 사주 입력, 수정, 삭제 팝업 기반 관리
- 직접 타이핑 가능한 생년월일 입력
- 오늘 운세 / 연간 운세 / 타로 / 궁합 / AI 상담 허브 구조
- 연간 운세 월별 밀도 보강
- 타로 3카드 구조
- 무료 3턴 상담 흐름
- 이메일 가입/로그인
- 카카오/구글 OAuth
- 인연 초대 링크 생성/수락
- 인연 카드에서 궁합/상담 바로가기
- 관리자 페이지 기본 운영 기능

### 최근 구현 내용

#### v0.5.58.0 궁합 기반 상담 시작 문장 자동화 + 상태 문서 정리

- 같은 인연 카드에서 방금 본 궁합 결과가 있으면, 그 한 줄 요약과 관계 흐름, 다음 행동이 상담 시작 문장에 자동으로 반영되도록 보강했다.
- 다른 관계를 새로 선택하면 이전 궁합 결과가 섞이지 않도록 궁합 컨텍스트와 상담 시작 문장을 함께 초기화하도록 정리했다.
- `PROJECT_STATUS_README.md`와 `CHANGELOG.md`를 현재 상태 기준의 정상 한글 문서로 다시 정리했다.

#### v0.5.57.0 인연 카드 궁합 컨텍스트 연결 보강

- 연결된 인연 카드에서 궁합으로 들어오면 선택된 관계가 상단 배너와 카드 선택 상태로 유지되도록 보강했다.
- 궁합 결과에서 같은 관계 맥락으로 바로 상담을 시작할 수 있는 CTA를 추가했다.
- 연결된 관계가 사라지거나 선택을 해제하면 궁합 컨텍스트도 함께 정리되도록 상태 흐름을 묶었다.

#### v0.5.56.0 궁합 결과 흐름형 확장

- 궁합 결과 카드에 관계 흐름, 다음 행동, 후속 질문 블록을 추가했다.
- 연인/부부/재회/가족 관계별로 서로 다른 후속 질문과 상담 연결 문장을 제공한다.

#### v0.5.55.0 타로 무료 콘텐츠 3카드 확장

- 타로를 핵심 카드, 보조 카드, 경계 카드 구조로 확장했다.
- 주제별 후속 질문과 마무리 문장을 추가했다.

## 일일 운세 알고리즘

일일 운세는 서버 호출 없이 브라우저 안에서 생성한다.

기본 원칙은 다음과 같다.

1. 입력된 사주 차트에서 오행 균형, 일주, 십신 등 핵심 신호를 뽑는다.
2. 현재 날짜를 기준으로 일일 키를 만든다.
3. 사주 신호와 날짜 키를 섞어 매일 바뀌는 점수와 문장 조합을 만든다.
4. 총점, 세부 영역 점수, 한 줄 요약, 행동 제안, 상담 연결 질문까지 같이 생성한다.

구현 위치:

- [src/daily.js](C:\WorkSpace\메모\10. Project\Saajuu\src\daily.js)
- 일부 해석 조립은 [src/fortune.js](C:\WorkSpace\메모\10. Project\Saajuu\src\fortune.js)에서 재사용

## 한자 성명학 데이터 파이프라인

현재 한자 성명학은 브라우저에서 동작 가능한 정적 데이터 기반 구조다.

구성은 다음과 같다.

1. 이름 입력
2. 한글 이름 정규화
3. 후보 한자 데이터 로드
4. 선택된 한자 기준 획수/오행/보조 해석 계산
5. 결과를 사주 본문과 별도 보조 리딩으로 노출

관련 자산:

- 이름 해석 로직: [src/fortune.js](C:\WorkSpace\메모\10. Project\Saajuu\src\fortune.js)
- 분리 번들 데이터: `dist/assets/name-hanja-*.js`

원칙:

- 원본 참고 데이터는 공개 저장소에 넣지 않는다.
- 브라우저에서 필요한 최소 형태만 번들로 사용한다.

## 인증 / 상담 / 결제 상태

### 인증

- 이메일 가입/로그인 동작
- 카카오/구글 OAuth 동작
- Supabase 세션 기반 마이 페이지 연결

### 상담

- 무료 3턴 체험 가능
- 페르소나 선택과 모드 선택 분리
- 관계 링크에서 상담 시작 문장 연결 가능
- 실제 LLM 응답 품질은 서버 프롬프트와 모델 설정 영향이 크므로 지속 보정 대상

### 결제

- PortOne 브라우저 SDK 연결
- 결제/복구/상담권 관리의 기본 운영 경로 존재
- 운영 비밀키와 웹훅 시크릿은 Edge Function Secret으로 관리

## 안전 / 법적 가드레일

- 생년월일시를 정적 공유 링크에 직접 넣지 않는다.
- 브라우저 코드에 API 키, 비밀키, LLM 키를 넣지 않는다.
- 의료, 투자, 법률, 임신, 생사처럼 단정적이거나 위험한 예언형 표현을 쓰지 않는다.
- 상담 결과는 한 줄 결론 뒤에 근거와 다음 행동이 이어지는 형태로 쓴다.
- 경쟁 서비스의 문구/레이아웃/브랜드를 복제하지 않는다.

## 현재 남은 우선순위

### 1. 상담 시작 문장 품질 보강

- 궁합 결과를 바탕으로 상담 시작 문장이 더 구체적으로 자동 채워지도록 보강 필요
- 같은 관계 링크에서 들어온 경우 요약, 관계 흐름, 다음 행동이 첫 질문에 자연스럽게 섞여야 함

### 2. 인연 UX 마찰 추가 축소

- 개인정보 보호 때문에 상대 생년월일 재입력은 유지
- 다만 왜 다시 입력하는지, 지금 어떤 관계를 보고 있는지, 이후 무엇이 이어지는지를 더 분명하게 보여줄 필요가 있음

### 3. 문서 / 인코딩 정리

- 예전 세션에서 깨진 한글이 일부 문서에 남아 있었음
- 현재 이 문서는 재작성했지만, 필요 시 다른 오래된 문서도 같은 방식으로 정리할 것

## 보류된 작업과 착수 조건

### PWA

- 착수 조건: 재방문 지표와 알림 수요가 실제로 확인될 것

### SEO 정적 페이지 대량 생성

- 착수 조건: 커스텀 도메인, 사이트맵, 빌드 파이프라인 방향 결정

### 다중 프로필

- 착수 조건: 한 계정이 여러 프로필을 반복 조회하는 실제 사용 신호 확인

## 작업 규칙 요약

- 변경 전후로 테스트와 빌드를 확인한다.
- 작업이 끝나면 `CHANGELOG.md`, `VERSION`, `package.json`, 이 문서를 갱신한다.
- `main` 푸시 전에는 사용자가 실제로 푸시를 원한다는 맥락이 있어야 한다.
